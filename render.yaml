services:
  - type: web
    name: baby-monitor-signaling
    runtime: node
    plan: free
    rootDir: src/infra/server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
