services:
  - type: web
    name: baby-monitor-signaling
    runtime: node
    plan: free
    buildCommand: cd src/infra/server && npm install
    startCommand: cd src/infra/server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
